{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "node -e \"process.env.TEST_MONGODB_URI = process.env.TEST_MONGODB_URI || 'mongodb://127.0.0.1:27017/workly-test'; const { spawnSync } = require('node:child_process'); const result = spawnSync(process.execPath, ['--test', '--test-concurrency=1', 'tests'], { stdio: 'inherit', env: process.env }); process.exit(result.status ?? 1);\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "argon2": "^0.45.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
